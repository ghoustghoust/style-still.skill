# style-still 协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。
> 同目录的 `CLAUDE.md` 只含一行 `@AGENTS.md`，让 Claude Code 读取同样的规则；Codex 直接读本文件。

## 项目一句话

style-still（文风蒸馏所）：双模式 Agent Skill——把一本书的文风蒸馏为《文风档案》，再按档案 spec 式分步写作/润色（公众号长文、章节、社媒短文、金句）。

> 本文件由 `cairn init` 生成，已写入本项目自身的定位与 provider 配置；其他项目请各自运行 init，不要直接复用。

## Init configuration

- 毕业目标 provider：暂缓对接（首次毕业时再连接知识库）
- 知识库索引：尚未配置
- 毕业写入目标：尚未配置

## 进入项目后的阅读顺序

1. 先读本文件（AGENTS.md）。
2. 若存在 `cairn/ROADMAP.md`，读取路线图、当前焦点和悬而未决的问题（ROADMAP 为可选项）。
3. 读 `cairn/LOG.md` 最新条目（新条目在最上方），了解近期进展和关键决策。
4. 按当前任务需要，读相关的 `cairn/` 主题笔记。

## 文档职责

| 文件 | 职责 | 维护方式 |
|---|---|---|
| `AGENTS.md`（根目录） | 规则与导航 | 很少变动，≤ 60 行 |
| `CLAUDE.md`（根目录） | 一行 `@AGENTS.md` 桩文件 | 写一次，不再动 |
| `cairn/ROADMAP.md` | 路线图与进度 | 原地更新，保持简洁 |
| `cairn/LOG.md` | 时序日志 | 新条目加在顶部（最新在前），每条 ≤ 20 行，只留摘要+指针 |
| `cairn/<主题>.md` | 知识主题笔记（当前真相） | 原地更新；踩坑记正文区，用 `contains` 标注；修正判断时在 LOG 留指针 |
| `cairn/Reference/` | 外部原始材料 | 需要时才建，只增不删 |
| `cairn/Cited.md` | 知识库引用清单 | 只挂指针，不抄正文 |

> 其他一切文档都等真实信号出现时才创建（有决策要记录、有坑被解决、有目标跨会话）——不预建空壳。被代码或流程直接消费的工程资产（契约/配置/规范）不属于本系统，留在代码树里，不进 `cairn/`。

## 冲突仲裁规则

- 优先级：**主题笔记 > LOG 历史**；规则级冲突以本文件为准。
- 业务/设计结论以 `cairn/` 主题笔记的最新记录为准，不以更早的 LOG 条目为准。

## 知识库消费反射

- 开始「产出或依赖的结论值得毕业」的工作前，先查本项目自己的 `cairn/` 主题笔记；当前尚未连接外部知识库（provider 暂缓对接——见上方 Init configuration），外部索引检查和 `cairn/Cited.md` 引用将在连接后激活。

## 文档协作规则

- 改动前判断用户想要「先讨论/建议」还是「直接改」；用户说「先看一下/先评估」时先给分析，不要直接重写正式文档。
- 修正过去的判断时追加更正说明，不要静默覆盖。
- 未确认的判断不要写成既定事实。

## 知识蒸馏规则

- 每次实质性推进后，在 `cairn/LOG.md` 顶部加一条记录（摘要+指针）；结论沉淀到 `cairn/` 主题笔记。
- 跨项目可复用的经验，待知识库连接后通过毕业机制蒸馏（provider 暂缓对接——见上方 Init configuration）。
